# Verified working on macOS / Apple Silicon (MPS), Python 3.11.
# Pinned to the versions used to develop plan3. Looser pins are usually fine,
# but these are known-good if you hit an API mismatch.
torch==2.12.0
transformer_lens==3.3.0
sae_lens==6.44.2
numpy==2.4.6
pandas==3.0.3
matplotlib==3.10.9
pyyaml==6.0.3
transformers==5.9.0
huggingface_hub==1.17.0
